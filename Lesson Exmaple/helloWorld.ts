const message:string = 'Hello World! ';
let seniority:number;
seniority = 30;
const decleration:string = `I've been a TS developer for ${seniority} seconds!`;

const finalMessage = message + decleration;